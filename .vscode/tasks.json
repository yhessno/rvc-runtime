{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Install snakeviz and run profiling",
            "type": "shell",
            "command": "pip install snakeviz && python -m cProfile -o profiling/temp.dat onnx_inference_demo.py",
            "problemMatcher": []
        }
    ]
}